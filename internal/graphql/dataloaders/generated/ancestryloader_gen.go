// Code generated by github.com/vektah/dataloaden, DO NOT EDIT.

package generated

import (
	"sync"
	"time"

	"github.com/eveisesi/athena"
)

// AncestryLoaderConfig captures the config to create a new AncestryLoader
type AncestryLoaderConfig struct {
	// Fetch is a method that provides the data for the loader
	Fetch func(keys []uint) ([]*athena.Ancestry, []error)

	// Wait is how long wait before sending a batch
	Wait time.Duration

	// MaxBatch will limit the maximum number of keys to send in one batch, 0 = not limit
	MaxBatch int
}

// NewAncestryLoader creates a new AncestryLoader given a fetch, wait, and maxBatch
func NewAncestryLoader(config AncestryLoaderConfig) *AncestryLoader {
	return &AncestryLoader{
		fetch:    config.Fetch,
		wait:     config.Wait,
		maxBatch: config.MaxBatch,
	}
}

// AncestryLoader batches and caches requests
type AncestryLoader struct {
	// this method provides the data for the loader
	fetch func(keys []uint) ([]*athena.Ancestry, []error)

	// how long to done before sending a batch
	wait time.Duration

	// this will limit the maximum number of keys to send in one batch, 0 = no limit
	maxBatch int

	// INTERNAL

	// lazily created cache
	cache map[uint]*athena.Ancestry

	// the current batch. keys will continue to be collected until timeout is hit,
	// then everything will be sent to the fetch method and out to the listeners
	batch *ancestryLoaderBatch

	// mutex to prevent races
	mu sync.Mutex
}

type ancestryLoaderBatch struct {
	keys    []uint
	data    []*athena.Ancestry
	error   []error
	closing bool
	done    chan struct{}
}

// Load a Ancestry by key, batching and caching will be applied automatically
func (l *AncestryLoader) Load(key uint) (*athena.Ancestry, error) {
	return l.LoadThunk(key)()
}

// LoadThunk returns a function that when called will block waiting for a Ancestry.
// This method should be used if you want one goroutine to make requests to many
// different data loaders without blocking until the thunk is called.
func (l *AncestryLoader) LoadThunk(key uint) func() (*athena.Ancestry, error) {
	l.mu.Lock()
	if it, ok := l.cache[key]; ok {
		l.mu.Unlock()
		return func() (*athena.Ancestry, error) {
			return it, nil
		}
	}
	if l.batch == nil {
		l.batch = &ancestryLoaderBatch{done: make(chan struct{})}
	}
	batch := l.batch
	pos := batch.keyIndex(l, key)
	l.mu.Unlock()

	return func() (*athena.Ancestry, error) {
		<-batch.done

		var data *athena.Ancestry
		if pos < len(batch.data) {
			data = batch.data[pos]
		}

		var err error
		// its convenient to be able to return a single error for everything
		if len(batch.error) == 1 {
			err = batch.error[0]
		} else if batch.error != nil {
			err = batch.error[pos]
		}

		if err == nil {
			l.mu.Lock()
			l.unsafeSet(key, data)
			l.mu.Unlock()
		}

		return data, err
	}
}

// LoadAll fetches many keys at once. It will be broken into appropriate sized
// sub batches depending on how the loader is configured
func (l *AncestryLoader) LoadAll(keys []uint) ([]*athena.Ancestry, []error) {
	results := make([]func() (*athena.Ancestry, error), len(keys))

	for i, key := range keys {
		results[i] = l.LoadThunk(key)
	}

	ancestrys := make([]*athena.Ancestry, len(keys))
	errors := make([]error, len(keys))
	for i, thunk := range results {
		ancestrys[i], errors[i] = thunk()
	}
	return ancestrys, errors
}

// LoadAllThunk returns a function that when called will block waiting for a Ancestrys.
// This method should be used if you want one goroutine to make requests to many
// different data loaders without blocking until the thunk is called.
func (l *AncestryLoader) LoadAllThunk(keys []uint) func() ([]*athena.Ancestry, []error) {
	results := make([]func() (*athena.Ancestry, error), len(keys))
	for i, key := range keys {
		results[i] = l.LoadThunk(key)
	}
	return func() ([]*athena.Ancestry, []error) {
		ancestrys := make([]*athena.Ancestry, len(keys))
		errors := make([]error, len(keys))
		for i, thunk := range results {
			ancestrys[i], errors[i] = thunk()
		}
		return ancestrys, errors
	}
}

// Prime the cache with the provided key and value. If the key already exists, no change is made
// and false is returned.
// (To forcefully prime the cache, clear the key first with loader.clear(key).prime(key, value).)
func (l *AncestryLoader) Prime(key uint, value *athena.Ancestry) bool {
	l.mu.Lock()
	var found bool
	if _, found = l.cache[key]; !found {
		// make a copy when writing to the cache, its easy to pass a pointer in from a loop var
		// and end up with the whole cache pointing to the same value.
		cpy := *value
		l.unsafeSet(key, &cpy)
	}
	l.mu.Unlock()
	return !found
}

// Clear the value at key from the cache, if it exists
func (l *AncestryLoader) Clear(key uint) {
	l.mu.Lock()
	delete(l.cache, key)
	l.mu.Unlock()
}

func (l *AncestryLoader) unsafeSet(key uint, value *athena.Ancestry) {
	if l.cache == nil {
		l.cache = map[uint]*athena.Ancestry{}
	}
	l.cache[key] = value
}

// keyIndex will return the location of the key in the batch, if its not found
// it will add the key to the batch
func (b *ancestryLoaderBatch) keyIndex(l *AncestryLoader, key uint) int {
	for i, existingKey := range b.keys {
		if key == existingKey {
			return i
		}
	}

	pos := len(b.keys)
	b.keys = append(b.keys, key)
	if pos == 0 {
		go b.startTimer(l)
	}

	if l.maxBatch != 0 && pos >= l.maxBatch-1 {
		if !b.closing {
			b.closing = true
			l.batch = nil
			go b.end(l)
		}
	}

	return pos
}

func (b *ancestryLoaderBatch) startTimer(l *AncestryLoader) {
	time.Sleep(l.wait)
	l.mu.Lock()

	// we must have hit a batch limit and are already finalizing this batch
	if b.closing {
		l.mu.Unlock()
		return
	}

	l.batch = nil
	l.mu.Unlock()

	b.end(l)
}

func (b *ancestryLoaderBatch) end(l *AncestryLoader) {
	b.data, b.error = l.fetch(b.keys)
	close(b.done)
}
